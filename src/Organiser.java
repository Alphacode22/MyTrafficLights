import java.util.ArrayList;

public class Organiser {

    //First car in the journey


    /*
        Given the description of a city plan and planned paths for all cars in that city,
        optimize the schedule of traffic lights to minimize the total amount of time spent in
        trac, and help as many cars as possible reach their destination before a given
        deadline
     */

    //We have
    // Cars
    // Intersection - Time number
    //



    //We need
    //Lowest time number?
    //Find the names of the traffic light

    //Sorts the the traffic lights
    ArrayList<Car> carJourneys = Main.carJourneys;
    public void findTrafficLightToChange() {
        int d=0;
        for (int c = 0; c <= carJourneys.size(); c++){
            //if(carJourneys.get(c).)
        }
    }
}
